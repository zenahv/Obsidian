$\boxed{\text{Exercice 1}}$
### 1. 
#### (a)
On cherche à prouver $\forall(m,n)\in \mathbb{N}^{2}$, `f m n` se termine.
$\forall(m,n)\in \mathbb{N}^{2}$,
<u>Initialisation</u>,
Pour $(0,0)$, comme $n=0$, alors la fonction renvoie $2$ <small>(ligne 2)</small>
Soit $m \in \mathbb{N}$, et $n=0$, idem que ci dessus. 

<u>Correction</u> :
Montrons que $\forall(m,n)\in \mathbb{N}^{2}$, $H_{m,n}$ : "`f m n` se termine"
$\mathbb{N}^{2}$ muni de l'ordre lexicographique est inductif

<u>Initialisation</u> :
Soit $m \in \mathbb{N},n=0$
`f m 0` renvoie `2` car $n=0$, $H_{m,0}$.

<u>Hérédité</u> :
Soit $(m,n)\in \mathbb{N}^{2}$ tel que $n\geqslant 1$ et $\forall(m',n')<(m,n)$, $H_{m',n'}$.
Montrons $H_{m,n}$
`f m n` avec $n \neq 0$

<small><u>Cas 1</u></small>
Si $m=0$, `f` renvoie `2 * f 0 (n-1)`
Or $(0,n-1)<(0,n)$, donc par $H.I.$, `f 0 (n-1)` termine. Aussi `f 0 n = 2(f 0 (n-1) )` termine

<small><u>Cas 2</u></small>
Si $m \neq 0,$ `f` renvoie `f (m-1) (2*n)`
Or, $((m-1),2n)<(m,n)$, donc par $H.I.$, `f (m-1) (2*n)` termine. Aussi `f m n = f (m-1) (2*n)` termine.

<u>Conclusion</u> :
D'où $\forall(m,n)\in \mathbb{N}^{2},$ $H_{m,n}$

#### (b)
Montrons par récurrence sur $k$ que $\forall k\in \mathbb{N},\forall(m,n)\in \mathbb{N}^{2}$ tels que $k=3^{m}n,$ `f m n` termine
<u>Initialisation</u>
Soit $k=0,$ alors $n3^{m}=0$, donc $n=0$ et $\forall m\in \mathbb{N},$ `f m 0` renvoie $2$. 
Ainsi `f m n` se termine si $k=0$

<u>Hérédité</u>
Soit $(m,n)\in \mathbb{N}^{2}$ et $k=n 3^{m}$ tel que $k \geqslant 1$
Soit $k'=n'3^{m'}$, supposons que $\forall k' \in \textlbrackdbl 0;k-1 \textrbrackdbl$, `f m' n'` termine
Montrons que `f m n` termine 

<small><u>Cas 1</u></small> : $m=0$ 
donc $k=n\geqslant 1$. `f` renvoie `2 * f 0 (n-1)`, Or $n-1 \in \textlbrackdbl 0;n-1 \textrbrackdbl$, donc par $H.R.$, `f 0 (n-1)` se termine.

<small><u>Cas 2</u></small> : $m \neq 0$
donc $k=n 3^{m}$ qui est divisible par $3$ `f m n` $=$ `f (m-1) (2*n)`
On a un nouveau $k_{1}=2n3^{m-1}= \frac{2n3^{m}}{3}= \frac{2}{3}n 3^{m}= \frac{2}{3}k$
Or comme $k \geqslant 1$, $\frac{1}{2} k \in \textlbrackdbl 0;k-1 \textrbrackdbl$, donc par $H.R.$, `f (n-1) (2*n)` termine

<u>Conclusion</u> : 
Vraie.

#### (c)
 [[TD15_1.jpg]]


### 2. 


#### (a)
Soit $m \geqslant 1$ tel que $\forall n' \in \mathbb{N}$, `ack (m-1) n'` termine. 
Prouvons par récurrence sur $n$ que la fonction `ack m n` se termine.

<u>Initialisation</u> :
Si $n=0$, `ack m 0` = `ack (m-1) 1`, qui termine par hypothèse

<u>Hérédité</u>
Soit $n \geqslant 1$. Supposons que `ack m (n-1)` termine
Alors on pose $n'$=`ack m (n-1)` qui existe par HR
Par hypothèse, `ack m n`=`ack (m-1) n'` termine

#### (b)
Montrons par récurrence que $\forall m \geqslant 0$,
$H_{m}$ : $\forall n\geqslant 0$ ,`ack m n` termine 

<u>Initialisation </u> :
Si $m=0$, $\forall n \geqslant 0$, `ack 0 n` = `n+1` termine

<u>Hérédité</u> :
Soit $m \geqslant 1$ Supposons $\forall n' \in \mathbb{N}$,
$H_{m-1}$ : `ack (m-1) n'` termine <small><span style="color:purple"> voir 2. (a) </span></small>
Alors $\forall n\in \mathbb{N}$, `ack m n` termine

<u>Conclusion</u> :
Donc $\forall m \geqslant 0,\forall n \geqslant 0$, `ack m n` termine.



$\boxed{\text{Exercice 2}}$

### 1. 
`fusion l1 l2` prend en entrée deux listes triées `l1` et `l2` et renvoie une permutation triée de la concaténation  `l1@l2`

### 2.
Montrons par induction sur les couples de listes `(l1,l2)`munis de l'ordre produit des ordres structurels que $\forall($`l1,l2`$)$ : 
`'a list` triées, `fusion l1 l2` est une `'a list` triée et une permutation de `l1@l2`
$(\text{C'est bien un ordre bien fondé comme produit d'ordres bien fondés})$
<u>Initialisation</u> :
Si `l1`=`[]`, $\forall$ `l2`:`'a list` triée, `l2`$=$`[]@l2` donc 
`fusion [] l2` est bien triée et est une permutation de `[]@l2`
De même si `l2`$=$`[]`, $\forall$`l1`:`'a list` triée

<u>Hérédité</u> :
Soient `l1`,`l2` des `'a list` triées non vides telles que $\forall($`l1',l2'`$)\prec($`l1,l2`$)$
Si `l1` et `l2` sont triées <small><span style="color:turquoise"> (en fait, ceci est toujours vrai) </span></small>, alors `fusion l1' l2'` est triée et est une permutation de `l1'@l2'`
`l1` et `l2` sont non vides, on pose `l1=x1::q1`, `l2=x2::q2`
`l1` est triée donc `x1` est le min de ses éléments
`l2` est triée donc `x2` est le min de ses éléments

<u><small>Cas 1</small></u> :
`x1` $\leqslant$ `x2`
`x1` $\leqslant \min($`l2`$)$ Donc `x1`$=\min($`l1@l2`$)\leqslant\min($`q1@q2`$)$
De plus, en posant `l1'`$=$`q1`$<$`l1` qui est triée, et `l2'`$=$`l2`
on a par $H.I.$ que `fusion q1 l2` est triée et est une permutation de `q1@l2`
Comme `x1`$\leqslant \min($`fusion q1 l2`$)$, `x1::fusion q1 l2` est triée
De plus, c'est une permutation de `x1::(q1@l2)`$=$`(x1::q1)@l2`$=$`l1@l2`

### 3.
Y'a moins de quantificateurs

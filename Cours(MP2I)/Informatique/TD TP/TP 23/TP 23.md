$\cancel{ (i_{2},\dots,i_{k}) }\to(i_{1}',\dots,i_{k-1}')$ 
tel que $\forall i\in\textlbrackdbl 0;n-i_{1} \textrbrackdbl, d_{i}'=d_{i+i_{1}}-d_{i_{1}}$
$\forall j\in\textlbrackdbl 0;k-1 \textrbrackdbl,i_{j}'=i_{j+1}-i_{1}$
Donc $\forall j\in\textlbrackdbl 0;k-1 \textrbrackdbl,d_{i_{j}'}'=d_{i_{j+1}}-d_{i_{1}}$ 

# 2
## a

Soient $n,m,(d_{0},\dots,d_{n})$ comme dans l'énoncé ,$(i_{1},\dots,i_{k})$ strict. croissant dans $\textlbrackdbl 1;n \textrbrackdbl$ tq :
$\begin{cases}d_{i_{1}}\leqslant m \\ (i_{1}',\dots,i_{k-1}')\text{ solution de }(m,(d_{0}',\dots,d_{n-i_{1}}'))\end{cases}$
En posant $i_{0}'=0$
Alors $\forall j\in\textlbrackdbl 0;k-2 \textrbrackdbl,d_{i_{j+1}'}'-d_{i_{j}'}'\leqslant m$
$\forall j\in\textlbrackdbl 0;k-2 \textrbrackdbl,(d_{i_{j+2}}\cancel{ -d_{i_{1}} })-(d_{i_{j+1}}\cancel{ -d_{i_{1}} })\leqslant m$
De plus, $i_{0}=0$ et $d_{0}=0$ par définition, donc $d_{i_{1}}-d_{i_{0}}=d_{i_{1}}\leqslant m$
Donc $\forall j\in\textlbrackdbl 0:k-1 \textrbrackdbl,d_{i_{j+1}}-d_{i_{j}}\leqslant m$

De plus, $i_{k}=i_{1}+i_{k-1}'=i_{1}+n-i_{1}=n$
car $(i_{1}',\dots,i_{k-1}')$ est solution d'un problème avec $n-i_{1}$ stations
Donc $(i_{1},\dots,i_{k})$ est bien solution de $(m,(d_{0},\dots,d_{n}))$

## b
Soit $(m,(d_{0},\dots,d_{n}))$ comme dans l'énoncé, $(i_{1},\dots,{i_{k}})$ solution du problème
Nécessairement, $i_{1}',\dots,i_{k-1}'$ est solution de $(m,(d_{0}',\dots,d_{n-i_{1}}'))$
Supposons que $(i_{1}',\dots,i_{k-1}')$ n'est pas optimale pour ce sous-problème
Soit $(\tilde{i}_{1}',\dots,\tilde{i}_{\tilde{k}-1}')$ solution optimale avec $\tilde{k}< k$
On a toujours la convention $\tilde{i}_{0}=0$
Posons pour $j\in\textlbrackdbl 1;k \textrbrackdbl,\tilde{i}_{j}=\tilde{i}_{j-1}+i_{1}$
D'après 2) a), $(\tilde{i}_{1},\dots,\tilde{i}_{\tilde{k}})$ est solution de $(m,(d_{0},\dots,d_{n}))$
Or $\tilde{k}< k$, donc $(i_{1},\dots,i_{k})$ n'est pas optimale

## c
Soit $(i_{1},\dots,i_{k})$ solution de $(m,(d_{0},\dots d_{n}))$ avec $k\geqslant 2$
On suppose $d_{n}> m$ (sinon le choix glouton est (n) qui est optimal)
Posons $j$ le plus petit entier tel que $d_{i_{j}}> m$
$i_{k}=n$ et $d_{n}> m$
De plus, $d_{i_{1}}\leqslant m$ donc $j\geqslant 2$
Soit $i_{1}'$ le choix glouton, c'est à dire le + grand entier $i$ tel que $d_{i}\leqslant m$
Nécessairement $d_{i_{j-1}}\leqslant m$ par définition de $j$ donc $d_{i_{j-1}}\leqslant d_{i_{1}'}$ par maximalité de $i_{1}'$
Donc $d_{i_{j}}-d_{i_{1}'}\leqslant d_{i_{j}}-d_{i_{j-1}}\leqslant m$
Comme $d_{i_{1}'}\leqslant m,$ on a bien que $(i_{1}',i_{j},i_{j+1},\dots,i_{k})$ est bien une solution au moins aussi bonne que $(i_{1},\dots,i_{k})$

## d
Il suffit pour montrer la correction de `arrets_glouton` de montrer que `arrets_aux d_i i`  calcule une solution optimale du problème $(m,(d_{i}-d_{i},d_{i+1}-d_{i},\dots,d_{n}-d_{i}))$
En effet, on voit aisément que `arrets_aux d_i_j i_j = d_i_j+1 :: arrets_aux d_i_j+1 i_j+1`
où `i_j+1-i_j` est le choix glouton pour le problème ci-dessous

On commence par remarquer que par définition ($1,\dots,n$) est solution du problème $(m,(d_{0},\dots,d_{n}))$, donc l'ensemble des stations est fini et non vide, donc il existe une solution optimale.

Montrons par récurrence sur $k$ la taille des solutions optimales de $(m,(d_{0},\dots,d_{n}))$ que `arrets_aux` calcule une telle solution optimale

<u>Initialisation</u>
Si $k=0,n=0,$ solution triviale $()$
Si $k=1$, alors nécessairement la seule solution optimale est ($n$), donc $d_{n}\leqslant m$ et $d_{n }'+\text{départ}=\text{station.(n)}$ est bien ce que renvois l'algorithme

<u>Hérédité</u>
Soit $k\geqslant 2$, supposons que pour tout problème dont une solution optimale est de taille $k-1$
L'algorithme calcule une telle solution
(et renvoie les distances sans décalage)
Soit $(m,(d_{0},\dots,d_{n}))$ un problème dont les solutions optimales sont de taille $k$
Soit $(i_{1},\dots,i_{k})$ une telle solution. $k\geqslant 2$ donc $d_{n}> m$
D'après 2) c) si $\tilde{i}_{1}$ est le choix glouton, 
alors 